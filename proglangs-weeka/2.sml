val y = 34;
val x = 17;
val q = if y > 0 then 0 else 42;
val a = ~5; (*negative numbers with ~ instead of -*)
val funny = 34; (*fun is service value*)
val w = 0;
val v = x div (w + 1); (*div instead of /*)